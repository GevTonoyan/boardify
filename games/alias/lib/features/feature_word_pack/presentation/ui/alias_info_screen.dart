import 'package:flutter/material.dart';

class AliasWordPackScreen extends StatelessWidget {
  const AliasWordPackScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Alias Word Pack')),
      body: Center(child: Text(' Alias Word Pack Screen')),
    );
  }
}
